<%= form_for message, url: message_path(message) do |f| %>

		<%= f.collection_select :category_id, Category.all, :id, :title %>


		<%= f.submit 'Обновить категорию', class: 'btn btn-primary btn-xs' %>
<br>
<% end %>